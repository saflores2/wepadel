

<div class="cont-<%= message.user.id %>">
  <div class="message-box msg-<%= message.user.id %> " >
      <h6 class="name"> <%= message.user.name %> <i><%= message.created_at.strftime("%d/%m/%y %H:%M") %></i> </h6>
  <%= message.content %>
  </div>
</div>

